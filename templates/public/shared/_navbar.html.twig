<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('public_home') }}">Forum</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon">
        {# <i class="fas fa-bars fa-2x"></i> #}
      </span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('public_home') }}">Accueil</a>
        </li>
        {% if app.user  %}
            {% if app.user.roles[0] is same as "ROLE_AUTHOR" %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('author_home') }}">Espace Auteur</a>
                </li>
            {% endif %}
            {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_home') }}">Espace Admin</a>
                </li>
            {% endif %}
            {% if app.user.roles[0] is same as "ROLE_MODERATOR" %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('moderator_home') }}">Espace Modérateur</a>
                </li>
            {% endif %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
            </li>

        {% else %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_register') }}">Créer un compte</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
            </li>

        {% endif %}
    </div>
  </div>
</nav>