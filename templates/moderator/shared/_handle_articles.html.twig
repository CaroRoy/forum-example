<div class="col-12 mt-4">
    <div class="card">
        <div class="card-header d-flex align-items-center justify-content-between">
            <h3>Gérer les articles</h3>
        </div>
        <div class="card-body">
            {% if posts | length > 0 %}
                <p>Il y a {{  posts | length }} articles à vérifier.</p>
                <a class="btn btn-primary btn-sm" href="{{ path('moderator_post_unpublish_list') }}">Liste des articles (brouillon)</a>
            {% else %}
                <p>Il n'y aucun article.</p>
            {% endif %}
        </div>
    </div>
</div>